- name: play level vars
  hosts: local
  connection: local
  vars_files:
  - course.yaml
  tasks:
  - name: print vars
    ansible.builtin.debug:
      msg: "Course is {{ COURSE }}, and Trainer is {{ TRAINER }}"
